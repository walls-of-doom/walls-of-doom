#!/usr/bin/env bash
#
# This script provides a platform-independent launcher for for macOS and Linux.
#
# Change to the script directory.
cd -P -- "$(dirname -- "$0")"
# Execute the file.
./walls-of-doom
